  const BACKGROUND_COLOR = 'black';
  const FONT = "Lucida Console";
  const RETRO_GREEN = 'rgb(95, 218, 101)';
  const RETRO_GREEN_ALPHA = 'rgba(95, 218, 101, .75)';
  const WINNING_GOLD = 'rgb(255,215,0)';
  const LEFT_UP = 'w';
  const LEFT_DOWN = 's';
  const RIGHT_UP = 'o';
  const RIGHT_DOWN = 'l';
  const MERIDIAN_WIDTH_DIVISOR = 75;
  const PADDLE_WIDTH_DIVISOR = 50;
  const PADDLE_HEIGHT_DIVISOR = 6;
  const PADDLE_ACC = 1.0001;
  const PUCK_RADIUS_DIVISOR = 50;
  const PUCK_BASE_VEL = 10;
  const MIN_VELOCITY = .0001;
  const REFLECT_ACCELERATOR = .5;


var constants =  {
  BACKGROUND_COLOR : BACKGROUND_COLOR,
  FONT : FONT,
  RETRO_GREEN : RETRO_GREEN,
  RETRO_GREEN_ALPHA : RETRO_GREEN_ALPHA,
  WINNING_GOLD : WINNING_GOLD,
  LEFT_UP : LEFT_UP,
  LEFT_DOWN : LEFT_DOWN,
  RIGHT_UP : RIGHT_UP,
  RIGHT_DOWN : RIGHT_DOWN,
  MERIDIAN_WIDTH_DIVISOR : MERIDIAN_WIDTH_DIVISOR,
  PADDLE_WIDTH_DIVISOR : PADDLE_WIDTH_DIVISOR,
  PADDLE_HEIGHT_DIVISOR : PADDLE_HEIGHT_DIVISOR,
  PADDLE_ACC : PADDLE_ACC,
  PUCK_RADIUS_DIVISOR : PUCK_RADIUS_DIVISOR,
  PUCK_BASE_VEL : PUCK_BASE_VEL,
  MIN_VELOCITY : MIN_VELOCITY,
  REFLECT_ACCELERATOR : REFLECT_ACCELERATOR
};

export {constants};
